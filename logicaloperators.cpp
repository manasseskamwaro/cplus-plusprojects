//AND OR NOT== && || !
//&&-combines two expressions if one is false then both are false
//|| -either
//!-inverter
//bitwise-&(and gate) |(or gate) ^(xor) >> << ~(inverter) they operate with respective gates
//assignment==a+=5
//conditional ?
//(a>b)?a:b
//type casting
//